1. Install the required python packages via pip install requirements.txt
2. Run the functions in main.py 
    - Please uncomment only the specific methods you would like to run. Otherwise, the whole process will take a long time
    - The output of the script is located in the results folder
3. Note: 
    -Do not delete the feature-ranks folder in the results folder
    -Everything else in the results folder can be regenerated by running the function in the main.py. 

1. pip install requirements.txt 명령어로 필요한 파이썬 패키지를 설치하세요. pip install -r requirements.txt
2. main.py 파일의 함수를 실행하세요.
- 실행하려는 특정 메서드만 주석 해제하세요. 그렇지 않으면 전체 프로세스가 오래 걸립니다.
- 스크립트 출력은 results 폴더에 저장됩니다.
3. 참고:
- results 폴더 내 feature-ranks 폴더는 삭제하지 마세요.
- results 폴더의 다른 모든 파일은 main.py의 함수를 실행하여 재생성할 수 있습니다.

-----------------------
1. 가상환경 생성
2. 가상환경 활성화 및 파이썬 3.7로 지정 설치
3. 가상환경 환경변수 설정: $env:PYTHONPATH = "D:\Semi2\INT\Personalized_Stress_Estimator" 올바른 절대 경로로 수정
4. 필요한 모듈 및 라이브러리 설치: pip install -r requirements.txt
5. 프로젝트 루트에서 실행: python -m src.main


